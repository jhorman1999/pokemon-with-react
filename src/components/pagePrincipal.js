import Papa from 'papaparse';
import datos from '../data/data.json'

const Pokemos = () => {
    
    console.log(datos)
    return (
        <div>
        <h1>hola</h1>
        </div>
    )

}

export default (Pokemos)